=== [May 23, 2026] ===
Verification & Observations on Application Status:

* [VERIFIED] Session Save State & Connection Mapping:
  - Resolved arrow enums mismatch: Mapped enums (ArrowType.NONE and ArrowType.STANDARD) to DB boolean columns (arrow_start/arrow_end) using custom connection mappers (connectionToDbRow, connectionFromDbRow) in `src/utils/connectionDb.ts`.
  - Upgraded SyncEngine robustness: Added `_hydrated` check preventing premature/empty saves of unhydrated canvas states. Implemented backoff-retry loop and complete snapshot restoration on error (dirty cards, connections, file system nodes, and deleted IDs are preserved and retried rather than lost on failure).

* [VERIFIED] Redesigned AI Chat, BYOK Enforcement & Multi-Model Agentic Support:
  - Bring Your Own Key (BYOK) Security: Implemented AES-GCM (256-bit) local storage encryption in `utils/apiKeyStorage.ts` utilizing a device-specific UUID secret and user ID scopes. This replaces plain-text keys with secure, encrypted storage.
  - Redesigned AIChat UI: Upgraded chat to a sleek, resizable panel with a robust multi-provider dropdown picker supporting Google (Gemini 2.5 Flash, Gemini 2.0 Flash), OpenAI (GPT-4o), and Anthropic (Claude 3.5 Sonnet).
  - Enforced BYOK Gating: Gated model execution and the "AI Brainstorm" button (which continues to require Gemini for Imagen) behind user-provided keys via a beautiful, interactive APIKeyModal that requests the correct provider key.
  - Seamless Multi-Model Thread Handoff: Implemented thread briefing summaries (`summarizeChatHandoff`) that contextually transfer chat intent and canvas alterations when the user switches LLMs mid-session, maintaining agent continuity.
  - Multi-Provider Agentic Properties: Designed a local RAG loop in `aiService.ts` where actions like "search_cards" and "read_card" are intercepted and resolved locally via `embeddingService.ts` before recursively feeding data back to the model (up to a depth of 3), enabling fully agentic behavior on *any* user-selected LLM.

* [VERIFIED] Master PDF Function Upgrade:
  - Fixed content parser bugs: Completely replaced the broken child-node text extraction logic with a robust `DocBlock` parser in `utils/pdfContentParser.ts` that successfully extracts headers, text paragraphs, lists, and code blocks from Slate-like JSON card content.
  - Structured clustering & formatting: Implemented card connectivity clustering (adjacency DFS) to group connected ideas logically. Generates an structured Table of Contents, orders body cards by coordinate-flow, formats code blocks and lists with indentation, and cleanly partitions empty structural card headers into an appendix.
  - Gaps remaining (Much to do): PDF generation does not yet render canvas pen strokes or drawings, ignores card media attachments (images/uploaded PDFs), and lacks customized page footers/numbering or customized branding styles.

* [VERIFIED] Drastic Session Load Time Improvements:
  - Lightweight Dashboard Loading: Refactored dashboard fetch to use `fetchSessionSummaries` in `services/sessionLoader.ts`. It extracts only essential lightweight metadata and preview cards, bypassing heavy JSON stroke arrays, connections, and files.
  - Instant Workspace Loading: When launching a session, `fetchFullSession` uses deferred queries for heavy strokes and chat history (`deferChat: true, deferStrokes: true`). This paints the canvas almost instantly.
  - Asynchronous Background Hydration: App.tsx automatically kicks off a background load (`fetchSessionHeavyData`) after the workspace paints, asynchronously merging strokes and chat history into state once ready.

* Card Interaction Enhancements:
  - Vertical Focus Navigation: Added registration for textareas inside `CardNode.tsx` enabling keyboard navigation (ArrowUp/ArrowDown) to shift focus between textareas of physically aligned cards on the canvas.
  - Caret-relative Image Pasting: Pasting images inside a card textarea now uploads the image to Supabase S3 and inserts the public URL `\n[URL]\n` exactly at the caret selection position, keeping editing context intact.
  - Memo updates: Updated `CardNode` memo comparison to include `image` and `fileName` changes, correcting a previous rendering lag where card visuals did not update.

=== [May 24, 2026] ===
Startup & Connection Hardening:
* Fixed hung port 3000 freeze: Diagnosed a critical port collision issue where a ghost `node.exe` process (PID 20068) from a previous run was hanging on port 3000. When launching the application locally via `npm run dev`, Vite was forced to start on port 3001, but the Electron shell was hardcoded to load `http://localhost:3000`, causing the browser/desktop window to hang on a blank/unresponsive screen forever. 
* Released port: Terminated the ghost Node.exe process, successfully releasing port 3000 so the dev server can mount on its designated port and connect immediately.
* Progressive dashboard load: Optimized the session summary fetch by querying only the indexed `session_id` column from the cards table, bypassing full card lists and content for all sessions on start. Mapped counts in-memory to preserve visual elements and counts, reducing dashboard card query times from 3.7 seconds to near-instantaneous. Incorporated lazy on-demand card fetching inside the dashboard AI generators when updating session covers/icons.
